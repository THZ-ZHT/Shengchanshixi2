# 2026-09-09 工作日志

## 多模态图像合成 Agent · 交互首页设计（完成）

- **画布**：`https://ardot.tencent.com/file/723720979899030`（抠取与光影一致合成Agent · 交互首页设计稿），5 画板全部完成：
  - State A 空状态上传（3:1）、State B 处理等待（3:38）、State C 交互舞台 63%（6:17）、State D 最终合成（6:82）、交互逻辑总规范（6:129）
  - 占位图按 placeholder 模式用 G() 灰框（用户允许 AI 生图，真实实现建议预生成 8 组关键帧）
- **视觉校验**：capture_layout 全 5 画板 + 1 次整体截图（5 frame 一次调用）+ 1 次确认截图。发现并修复 1 处真实缺陷：State C 底部 Agent 状态文字（264px 宽）与控制器 ORIGINAL 标签碰撞 → 缩短为「正在同步人物高光与发丝边缘。」。其余 overlap 警告均为有意分层（进度填充/光晕/扫描线/节点圆点），LARGE_EMPTY_AREA 均判定良性。
- **方案文档**：`D:\ZHT\生产实习\前端整体方案_多模态图像合成Agent.md` —— 单一进度源 Composition Controller、四态状态机（Empty/Processing/Interactive/Final）、八模块拆解（M1 Header ~ M8 Final/AI CHECK）、鼠标五式交互（拖/移/悬/点/滚）、Voice Presence 三态波形振幅表、七阶段管线 0/12/28/44/63/76/90/100、React+TS+GSAP 技术方案、P0-P3 开发优先级。
- **经验**：C() 拷贝后代需「裸拷贝→batch_read 取新 ID→U() 更新」；capture_layout 的 parentId 不能传页面根节点（传具体 frame）。

## V1.1 · 按 2.txt 规范修改与标注（同日完成）

- 用户新增 `2.txt`（40 节前端架构）→ 设计稿全画板改造 + 新增第 6 块「实现标注 · Build Spec」画板（9:11, x=7800）
- 阶段改区间制（Compose 28–45 / Illuminate 45–65 / Ground 65–78 / Harmonize 78–92 / Critic 92–100）；Agent 严格三态（A 呼吸●去波形 / B 虚线环◌ / C·D 保留波形）；State B 步骤条 4→5（上传✓解码✓主体●场景○构图○）+ 82% 进度
- **AI 生图上链路**：ImageGen 文生图（室内原图）→ 图生图（同人物转夕阳户外，input_fidelity=high 保证相似）→ PIL 裁掉底部 80px 水印 → register_assets + curl PUT + upload_images 应用到 3:47/6:28/6:91（C/D 共用同图保证连续性）。注意：ImageGen 出图自带「AI生成 WORKBUDDY」右下水印，必须裁剪
- **修复**：Card Input(6:149) 被误挂到 State B（3:38）内 → M() 移回 6:129；步骤条新组追加后需 M(index) 调序；区间制长文案使节点 7/8 标签重叠 → 改三行堆叠
- 方案文档已更新至 V1.1（`前端整体方案_多模态图像合成Agent.md` 第九节变更记录）

## 效果图 1:1 复刻（同日）

- 用户指认 `微信图片_20260909000812_45_15.png` 为效果图 → 新建第 7 块画板「效果图复刻 · Main Showcase」（9:38, x=9360）完整复刻：Header/左上传栏/标题/四片扇形渐进/AI Agent 胶囊/56% 进度轨/7 Stages 圆点/底部七缩略图/右侧 Agent 对话+输入框+Quick Actions/叶影装饰
- 图片链路：文生图主图（侧脸+背包+山湖日落）→ 图生图派生灰度城市、白剪影 → PIL：裁水印（底部80px）、调色变体（desat/bright/dark/blur）、**自制棋盘格 Matte**（白剪影阈值做蒙版贴降饱和人物到棋盘格，用于 Extract 片）
- **修复**：七阶段圆点组件曾压到右侧对话栏（整体左移至 x=880-1110）；v_mask 图生图输出全黑图（改用 PIL 合成方案）；More Possifications 的 ↗ 字符触发 emoji 告警（改 SVG 箭头）
- 上传批量 curl 偶发 exit 23 但 HTTP 均 200（/dev/null 写出怪癖，非失败）；presigned URL 过期会 403，需重新 register

## 网站开发 · 前端实现启动（13:21 起）

- 团队 `software-imagecompose` 按 software-company SOP 推进：许清楚(PRD)→高见远(架构)→寇豆码(实现)→严过关(测试)，主理人中转落盘
- 工程位于 `frontend/`（ASCII 路径规范 N7）：React18+TS+Vite+GSAP+Zustand+Three.js+router；依赖经 npmmirror 安装 3min 成功；中文路径下 vite build 验证通过（782ms）
- PRD（简单版）已落盘 `frontend/docs/PRD.md`；资产契约确定：demo 关键帧 6 张（kf_gray_city/kf_subject RGBA/kf_matte/kf_silhouette/kf_final/kf_indoor，1536×854 对齐）+ bg 预设 4 张（bg_cafe/lake/city/forest，ImageGen 新生成 1536×934，全部 PIL 裁掉底部 90px 水印并 ASCII 重命名）
- **关键发现**：v_final 与 纯黑底 kf_subject 人物像素位置对齐（同源图生图高保真），可在 COMPOSE 段场景淡入时完美被主体层覆盖；光源约定改为"右侧落日"（与 v_final 太阳位置一致，LIGHT 面板 Direction≈212°）
- AI 3D 建模：文生3D（人物雕塑，face-count 60000）后台生成 GLB → public/assets/models/demo_subject.glb
- 经验：ImageGen output_dir 直接落盘到指定目录；图生3D 的 --image-base64 无法用（Windows 命令行 32K 限制，600KB base64 传不进去）→ 改文生3D

## 网站开发 · 前端实现完成与 QA 闭环（同日）

- **QA 回归 5 轮**：R1 发现 4 处异常 → 逐一定位：favicon 404（补 public/favicon.svg+link）、TYPED:false（input 缺 type="text"，补齐 AgentPanel×2+ImageGenPanel）、CLICK_MISS:森林晨光（脚本用词错误，实际预设 傍晚湖边/咖啡馆/城市夜色/森林）、26/27 截图与 25 逐字节相同（AI 面板内嵌左栏底部被推出视口+面板切换致滚动重置，脚本加 scrollTop 到底）。R5 全绿：consoleErrors=0、TYPED:INPUT、GLB_CANVAS:1、MESH_CANVAS:1。
- **实测证实的能力**：混元 17MB GLB 三维查看器真实渲染；本地图生 3D（主体图→高度图→位移网格）真实生成并自动旋转；输入框指令「光线太冷了」→ 5200K 局部重跑（relight n4-V3，背景 n2 不动）；森林预设 AI 生图合成（n2-V4）；条件回滚（换回上一版背景+保留光线+重跑下游 n7-V3）。
- **测试脚本经验（puppeteer-core）**：clickText 必须「交互元素优先」（button/a/[role]/.chip 先于 span/div，否则命中含同词的状态提示文字）；点击左栏底部面板后需 scrollTop=scrollHeight 再截图（React 卸载/挂载会重置滚动）；用 canvas 计数 + 截图双重取证 WebGL 渲染。
- **构建**：npm run build 通过（96 模块，three 688KB 独立 chunk 懒加载）；vite emptyOutDir 触发沙箱批量删除保护（阈值 50）→ 先手动 rm -rf dist 再构建。生产预览 http://127.0.0.1:4173（首页/favicon/GLB 全 200/206）。
- **QA 终判**：通过（PASS）——0 P0 / 0 P1 / 1 条 P2 记录型观察（AI 面板随资产增多被推出视口，可滚动到达，建议后续吸底，不阻塞）。
- **交付**：三页流程全通（Showcase 四态→Workspace 工作台→Research）；功能清单与使用流程已输出给用户。

## 前端升级：全量中文化 + 高级感 + 3D/交互增强（用户验收轮）

- **P0 中文化**：20+ 文件英文清零（eyebrow/FROM IMAGE TO REALITY/LIGHT/MATTE/Direction 等 → 中文），STAGE_LABELS 改中文并保留 STAGE_LABELS_EN 副本，CRITIC_SCORES key 改中文，DAG 卡片以中文为主（英文名降级为 title 提示），QA 脚本同步改'试用示例图'。
- **P1 视觉**：global.css v2（字阶/深色强调 --deep+--gold/分层阴影/glass/reduced-motion 全局兜底）；Showcase 深色金铜 FinalPanel、渐变标题、玻璃顶栏/语音条、入场 stagger、LIGHT 数值 lerp 缓动；工作台卡片分层、genPanel 吸底（顺带解决 QA P2 观察项）、DAG 版本号 versionPop 动画（key 重挂载重播）。
- **P2 3D**：LIGHT_SOURCE 常量 + kelvinToRgb → 3D 场景 DirectionalLight 与 2D 合成共用光源（212°/4800K/72%）；全屏三维场景模态；GLTFLoader onProgress 加载条；本地图生 3D 分辨率 96-256 可选 + 线框切换 + GLTFExporter 导出。
- **P3 交互**：画布「对比」模式（原图↔成片 clipPath 拖拽对比条）；指令输入 ↑/↓ 历史；舞台点击跳转原有保留。
- **坑**：compareHost 在 align-items:center 父容器里 flex:1 高度塌陷为 0 → align-self:stretch+min-height 修复；vite preview/dev 服务器会中途死掉，QA 前先 curl 探活。
- **验证**：tsc+vite build 通过（GLTFExporter 独立 chunk）；qa_run 25 步 consoleErrors=0，30_compare/31_3d_fullscreen/10_stateA/19_final 截图均确认。

## 前端升级 · 动效系统 + 真实图片上传（今日深夜轮）

- **工程师 429 中断**：software-engineer 在实现动效基础设施 11 分半后因模型频率限制失败；已产出 `src/utils/motion.ts`（revealIn/staggerIn/countUp/tilt/magnetic/ripple/floatLoop/drawStroke）、`src/hooks/useReveal.ts`、`src/components/common/AppBackground.tsx/css`、`global.css` keyframes + prefers-reduced-motion 兜底、Showcase 部分 CSS 动画。
- **主理人接管完成**：
  - **ResearchPage 动效接入**：`useReveal` 用于研究方向卡（stagger 90ms）、管线图容器、工具链/指标表行（55-60ms），滚动即揭示。
  - **FinalPanel 改造**：改用 `motion.countUp`（不逐帧 setState），五维评分条错峰 fadeUp + width transition，总分单独 count-up。
  - **Workspace 消息流**：`.msgRow` 加 `msgSlideIn` 入场动画（含 stagger）。
  - **真实图片上传链路**（用户核心诉求）：
    - `compositionStore` 新增 `userImage: { original, subject, final, width, height, name }`
    - 新建 `utils/imageMatting.ts`：浏览器内基于边缘颜色距离 + alpha 渐变 + 3×3 alpha 模糊完成轻量主体抠取
    - 新建 `utils/composePreview.ts`：将抠取主体覆盖到默认森林背景生成专属 Final 预览
    - `UploadCard` 支持点击/拖拽上传，阶段显示「读取图像 → 抠取主体 → 合成预览场景 → 准备合成」
    - `ShowcaseCanvas` 动态切换：有 userImage 时用用户原图/抠取主体/专属合成；无 userImage 时回退 demo 关键帧
  - **构建**：`tsc -b && vite build` 持续通过（99 模块）。
  - **QA 34 步零错误**：覆盖 demo 全七阶段、工作台指令/回滚/AI 生图/3D 建模/对比条，加上真实图片上传链路（`UPLOAD:OK`、`USER_IMG: data:image/jpeg...`），以及动效审计（首页 7 动画 / 工作台 22 动画 / 研究页 driftA/B）。
- **交付验证**：`34_user_final.png` 证实用户上传图（蓝底人形）走完七阶段后，终态为「森林背景 + 用户主体 + 五维 AI 自检 + 检查通过 95/100」，证明非示例图也能走通完整合成流程。

## 工作台联动上传图 + 过渡动画（本日凌晨收尾）

- **问题定位**：`workspaceStore.initDemoAssets()` 和 `services/orchestrator.produceArtifact()` 三处硬编码 `DEMO.grayCity / DEMO.matte / DEMO.final`，导致上传图片后进入工作台仍显示示例图。
- **修复**：
  - `initDemoAssets` 读取 `compositionStore.userImage`，有上传图时原图 = `user.original`、主体层 = `user.subject`；
  - `produceArtifact('n1')` 的 Alpha 遮罩改为 `user.subject`；
  - `produceArtifact('n6')` 的合成结果改为 `user.final`（森林背景 + 用户主体）。
- **过渡动画**：`CanvasPanel` 新增「播放过渡」按钮，GSAP 驱动 split 从 0→100→50，自动扫过对比条，左侧原图、右侧成片，直观展示变化。
- **验证**：QA 扩至 38 步，`WS_SOURCE` 显示 `source` 与 `result` 均为 base64 data URL，`35_user_workspace.png` 证实工作台左栏/中央/底部 DAG 全部基于上传图，`36_user_play.png` 清晰展示原图↔成片过渡动画。
- **生产构建**：`npm run build` 通过；`vite preview` 在 4173 重启。

## 高级 Agent 前端功能闭环（同日 · 深夜）

- **人物动画特效**：`types` 新增 `SubjectAnimation`，`workspaceStore.params` 加 `subjectAnimation` + `setSubjectAnimation`；`CanvasPanel` 加 6 种动画选择器，在结果图上叠加抠取主体并应用 CSS 关键帧（浮动/呼吸/脉动/旋转/行走/摆动）；`workspace.module.css` 追加 `@keyframes subjFloat/.../swing` + `prefers-reduced-motion` 兜底。
- **自然语言增强**：重写 `services/commandParser.ts`：扩展 `ParsedAction`（`light_kelvin` / `harmonize` / `animate` / `custom_bg` / `multi`）；增加关键词库与模糊匹配；支持组合意图（如「让人物轻轻浮动，并且把背景换成傍晚」拆成 multi [animate + switch_bg]，自动重跑下游 n4-n7）；`orchestrator.applyAction` 支持新动作类型。
- **自定义背景上传**：`ImageGenPanel` 新增隐藏 `input[type=file]` +「上传自定义背景图」按钮，读取 DataURL 后调用本地合成引擎，生成新的 background asset 和 result asset，走完整 DAG。
- **后端部署方案**：新文档 `frontend/docs/AGENT_BACKEND.md`：完整架构（Node.js Gateway + Python Sandbox + LLM + 工具调用 + WebSocket）、仓库结构、`llm/prompts.ts`/`ws.ts` 示例、模型清单与成本、Docker Compose、阿里云一键部署、前端切换说明、5 周时间表。
- **QA 扩至 48 步**：consoleErrors=0 / reqFail=0；截图 `40_anim_pulse.png`（人物脉动动画）、`44_nl_combo.png`（自然语言组合意图触发 DAG 重跑）、`45_custom_bg.png`（自定义背景上传合成成功）均确认。
- **交付**：生产构建通过；`vite preview` 在 4173 重启。
